# Session 2026-05-23 01:12 pass3-hardening

**Tool:** Claude Code
**Branch at end:** main
**Code baseline SHA at end:** a216c92
**Code worktree at end:** clean

## Done

- Reviewed `tmp/pass3-gpt.md` — the most source-grounded of the four iterations
  (Git, GitHub Actions, Docker, Postgres, Drizzle, Claude, Codex vendor docs
  cited throughout). The report endorses our architecture and suggests a few
  hardening tweaks plus an explicit acceptance test.
- Ran the false-positive-drift acceptance test pass3 calls out:
  `git log a45111d..HEAD --oneline -- ':!docs/agent' ':!AGENTS.md' ':!CLAUDE.md'`
  Result: returned `a216c92 chore(agent): bootstrap …`, i.e. a false code-drift
  alarm. Root cause: the bootstrap commit also touched `.gitignore` and
  `.github/workflows/deploy-staging.yml`, which aren't memory-only paths, so
  it counts as a non-memory commit and should be the baseline. My seed had
  `Code baseline SHA: a45111d` (pre-bootstrap) — wrong by one commit.
- Corrected `STATE.md` baseline → `a216c92` (computed mechanically with
  `git log -1 --format=%h -- ':!docs/agent' ':!AGENTS.md' ':!CLAUDE.md'`).
- Hardened the protocol in `AGENTS.md`:
  - `save session` step 2 now spells out the exact baseline-computation
    command so future saves can't drift the way the seed did.
  - `start session` step 4 uses `git status --porcelain=v1` for parse-stable
    output (pass3 recommendation citing the porcelain stability guarantee).

## Decisions

- Adopt pass3's mechanical baseline-computation command in the save protocol.
- Adopt `--porcelain=v1` for the start-session git status check.
- **Skip** pass3's verbose `:(top,glob,exclude)docs/agent/**` pathspec form —
  functionally equivalent to `:!docs/agent`, noisier to read. Short form
  stays.
- **Skip** adding `{{.HealthStatus}}` to verify-with templates — `{{.Status}}`
  already shows "(healthy)/(unhealthy)" parenthetically.
- **Skip** splitting save staging into "routine vs root-file" variants —
  overspecification; `git add docs/agent` (plus root files when actually
  touched) is sufficient.

## Failed approaches (avoid retrying)

- Initially set `Code baseline SHA` to the SHA that existed _before_ the
  bootstrap commit. Wrong: the bootstrap commit itself touched non-memory
  files (`.gitignore`, workflow), so it should be the baseline. The fix is
  to compute the baseline mechanically at save time, never eyeball it.

## Next

1. Re-run the acceptance test with the corrected baseline:
   `git log a216c92..HEAD --oneline -- ':!docs/agent' ':!AGENTS.md' ':!CLAUDE.md'` — expect empty (only memory commits since baseline). After this save lands the answer will still be empty because this save itself is memory-only.
2. Open fresh Codex and Claude sessions, type `start session`, confirm the
   drift check now reports clean (no false alarm).
3. `publish session` (three local memory commits queued: bootstrap, ECC
   reconciliation, this hardening).
4. The pre-rebaseline DB backups can be cleaned up whenever v2.5.0 has
   soaked enough.

## Resume prompt

The memory system is now hardened against the seed bug pass3 surfaced.
Three unpushed memory commits sit on main (a216c92, 132eb26, plus this
hardening save). The first concrete action is end-to-end verification:
open a fresh Codex CLI session in `/home/vladkarok/Work/email-to-telegram`,
type `start session`. Expected: the agent reads STATE.md (baseline a216c92)

- this session file, runs `git status --porcelain=v1` (clean) and the
  baseline diff (empty — no code commits since a216c92), runs the
  staging/prod `verify-with:` commands (image tags `:main` and `:v2.5.0`,
  both healthy, migration head `1779472699656`), and surfaces this resume
  prompt. Same in a fresh Claude Code session — the ECC `SessionStart`
  hook will fire and load an unrelated summary; the agent should ignore it
  per AGENTS.md standing rule #4 and follow this protocol. Once both pass,
  run `publish session` (= `git push`) — the staging workflow's
  `paths-ignore` for memory files means no deploy fires. Nothing else
  pending; next thread is user-driven.
